<UserControl x:Class="FoodExplorer.Views.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FoodExplorer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="PlaceholderTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5">
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              Focusable="false" 
                                              HorizontalScrollBarVisibility="Hidden" 
                                              VerticalScrollBarVisibility="Hidden"
                                              VerticalAlignment="Center"
                                              Margin="5,0"/>
                            </Border>

                            <TextBlock x:Name="PlaceholderText"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="Black"
                                       IsHitTestVisible="False"
                                       VerticalAlignment="Center"
                                       Margin="12,0"
                                       FontStyle="Italic"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Preparation for PDF export 📄" FontSize="22" FontWeight="Bold" Margin="0,0,0,15" Foreground="#2D3436"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,20" Height="35">
            <Button Content="Add to the list" 
                    Command="{Binding AddToExportCommand}" 
                    Style="{StaticResource RoundedButtonStyle}"
                    DockPanel.Dock="Right" 
                    Width="120" 
                    Margin="10,0,0,0" 
                    Foreground="White" 
                    Background="#FF1FB015" 
                    FontWeight="Bold"/>

            <TextBox Text="{Binding ExportSearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                     Tag="Enter barcode to add a product..." 
                     Style="{StaticResource PlaceholderTextBoxStyle}"
                     FontSize="13"/>
        </DockPanel>

        <ScrollViewer Grid.Row="2" Margin="0,0,0,15" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding TempExportList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#EEE" BorderThickness="1" Margin="0,3" Padding="12" CornerRadius="8" Background="#FAFAFA">
                            <DockPanel>
                                <Button Content="✕" 
                                        Command="{Binding DataContext.RemoveFromExportCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" 
                                        DockPanel.Dock="Right" 
                                        Background="Transparent" 
                                        BorderBrush="Transparent" 
                                        Foreground="#D63031" 
                                        FontWeight="Bold"
                                        Cursor="Hand"/>

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#2D3436" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding BarCode, StringFormat=' — [{0}]'}" Foreground="Gray" FontSize="11" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="3" 
                Content="GENERATE PDF REPORT" 
                Command="{Binding GeneratePdfCommand}"
                Style="{StaticResource RoundedButtonStyle}"
                Height="45" 
                Background="#7D332E" 
                Foreground="White" 
                FontWeight="Bold" 
                FontSize="14"/>
    </Grid>

</UserControl>
