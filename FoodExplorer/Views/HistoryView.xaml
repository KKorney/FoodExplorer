<UserControl x:Class="FoodExplorer.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="HistoryButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,7"/>
            <Setter Property="Background" Value="#FFEAD511"/>
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Recently Viewed 📜" FontSize="24" FontWeight="Bold" Foreground="#444" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding HistoryList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#EEE" BorderThickness="1" Margin="0,0,0,10" Padding="12" CornerRadius="8" Background="White">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/>
                            </Border.Effect>

                            <DockPanel LastChildFill="True">
                                <Button Content="👁️ View Details" 
                                        Style="{StaticResource HistoryButtonStyle}"
                                        Command="{Binding DataContext.ShowProductFromHistoryCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                        CommandParameter="{Binding Product}" 
                                        DockPanel.Dock="Right" 
                                        VerticalAlignment="Center"/>

                                <Image Source="{Binding Product.ImageFrontSmallUrl, TargetNullValue='/Assets/no_image_icon.png', FallbackValue='/Assets/no_image_icon.png'}" 
                                       Width="55" Height="55" Margin="0,0,15,0" Stretch="Uniform"/>

                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Product.Name}" FontWeight="Bold" FontSize="16" Foreground="#333"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                        <TextBlock Text="Last seen: " FontSize="11" Foreground="Gray"/>
                                        <TextBlock Text="{Binding ConsultationDate, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" 
               FontSize="11" 
               Foreground="#3674DA"
               FontWeight="SemiBold"/>
                                    </StackPanel>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>


</UserControl>