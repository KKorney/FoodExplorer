<UserControl x:Class="FoodExplorer.Views.ScannerView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:FoodExplorer.Views"
            mc:Ignorable="d" 
            d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="Black">
        <Image Source="{Binding CameraImage}" Stretch="UniformToFill">
            <Image.LayoutTransform>
                <ScaleTransform ScaleX="-1"/>
            </Image.LayoutTransform>
        </Image>

        <Border BorderBrush="Lime" 
                BorderThickness="3" 
                CornerRadius="15"
                Width="400" 
                Height="200" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Opacity="0.7">

            <Border.Effect>
                <DropShadowEffect Color="Lime" BlurRadius="10" ShadowDepth="0"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="[ ALIGN BARCODE ]" 
                           Foreground="Lime" 
                           FontWeight="Bold" 
                           FontSize="20" 
                           TextAlignment="Center"/>
                <TextBlock Text="Keep steady for 1-2 seconds" 
                           Foreground="White" 
                           FontSize="10" 
                           Opacity="0.8"
                           TextAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Button Content="Back to Search" 
                Command="{Binding DataContext.ShowSearchCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                HorizontalAlignment="Left" 
                VerticalAlignment="Top" 
                Margin="20" 
                Padding="15,8" 
                Background="#88000000" 
                Foreground="White"
                BorderThickness="1"
                BorderBrush="White">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</UserControl>